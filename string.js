const name = "chanda" 
const repoCount = 23

console.log(name + repoCount + " chandan" );
console.log(`name is ${name}  and rapo count is ${repoCount}` ); // this is called string interpolation

const string = new String("chandan")

console.log(string.toUpperCase());


const neName = "   chchcha   "


console.log(neName.trim());
console.log(neName);

